name: dev

on:
  workflow_dispatch:

jobs:
  testcase:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          repository: hiyouga/LLaMA-Factory
          path: .
      - name: preprocessing
        run: |
          set -x
          
          echo "hello world."
